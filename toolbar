# This file is basically useless and just prevent IceWM from following the default 
# toolbar file located in "/usr/share/icewm/toolbar", and adding some shortcuts, like
# "Web browser", "xterm", etc.
